import {test} from 'node:test';import assert from 'node:assert/strict';import {schemas,cleanPayload,validate} from '../src/cms/schema.js';
test('publish payload strips server fields and translation metadata',()=>{const p=cleanPayload('projects',{id:'x',updated_at:'old',title:'A',translations:{'zh-CN':{title:'甲',id:'spoof'}}});assert.equal(p.id,undefined);assert.deepEqual(p.translations,{'zh-CN':{title:'甲'}});});
test('reject invalid image and dangerous URLs before save',()=>{assert.equal(validate('projects',{title:'A',visibility:'public',link:'javascript:alert(1)'}).length,1);assert.equal(validate('personal_entries',{title:'A',kind:'daily',visibility:'public',images:[{url:'https://example.com/photo.jpg',alt:''}]}).length,1);});
test('accept real bilingual journal payload',()=>{assert.deepEqual(validate('personal_entries',cleanPayload('personal_entries',{title:'A',kind:'photography',images:[{url:'https://example.com/photo.webp',alt:'Trees'}],translations:{'zh-CN':{title:'甲'}}})),[]);});
test('admin manages the three experience tables independently',()=>{assert.deepEqual(Object.keys(schemas).filter(table=>table.endsWith('_experiences')),['work_experiences','club_experiences','volunteer_experiences']);});
